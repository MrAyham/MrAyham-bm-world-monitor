# BM World Monitor Stage 1 verification

Before merging `bm/integration` into this fork's `main`:

- [ ] TypeScript build passes.
- [ ] Default dashboard boots.
- [ ] HTML title/meta show BM World Monitor.
- [ ] Upstream attribution and AGPL notices remain intact.
- [ ] Existing `full`, `tech`, `finance`, `happy`, `commodity`, and `energy` variant contracts are not broken.
- [ ] No data source or map layer behavior changes unexpectedly.
- [ ] `VITE_BM_SHELL_URL` remains optional and does not break standalone operation.
